export const DEFAULT_PAGE_SIZE = 10;
export const MAX_PAGE_SIZE = 50;

export const FUEL_TYPES = [
  "Essence",
  "Diesel",
  "Hybride",
  "Électrique",
  "GPL",
] as const;

export const TRANSMISSION_TYPES = ["Manuelle", "Automatique"] as const;
